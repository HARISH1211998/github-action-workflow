name: Print GitHub Context on Issue Comment

on: [issue_comment, workflow_dispatch]

jobs:
  show-context:
    runs-on: ubuntu-latest
    steps:
      - name: Print GitHub context
        run: echo '${{ toJson(github) }}'
